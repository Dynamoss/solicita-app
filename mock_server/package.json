{
  "name": "solicita-mock-server",
  "version": "1.0.0",
  "private": true,
  "description": "Mock REST API (json-server) para o app de Acompanhamento de Solicitações",
  "scripts": {
    "start": "json-server --watch db.json --port 3000 --delay 300"
  },
  "devDependencies": {
    "json-server": "0.17.4"
  }
}
